# Intro
This is the dotfiles(configure files that are start with `.`, like `.gitconfig` e.t.c.) repo.

# Useage 
Use *stow* to generate the soft symbolic link to these files/directories.

```bash
stow -t desired_dir folder
```

This command will generate the soft symbolic link @ desired_dir
